<section class="showcase">
  <nav class="topbar">
    <div class="brand">
      <span>Duovilli</span>
    </div>
    <div class="nav-links">
      <a href="#esitykset">Esitykset</a>
      <a href="#tarina">Tarina</a>
      <a href="#tapahtumat">Tapahtumat</a>
      <a href="#yhteystiedot">Yhteystiedot</a>
    </div>
    <a class="primary-button" href="#yhteystiedot">Varaa esitys</a>
  </nav>

  <header class="hero">
    <div class="hero-copy">
      <p class="eyebrow">TANSITEATTERI • LIVE-ESITYS • KULTTUURI</p>
      <h1> Tanssiteatteria, joka jättää jäljen.</h1>
      <p class="lead">
        Duovilli yhdistää liikkeen, tarinan ja tunnelman tavalla, joka koskettaa katsojaa
        sydämen syvyyksistä. Jokainen esitys on elämyksellinen, intensiivinen ja mieleenpainuva.
      </p>
      <div class="actions">
        <a class="primary-button" href="#esitykset">Katso esitykset</a>
        <a class="secondary-button" href="#tarina">Lue tarina</a>
      </div>

    </div>

    <div class="hero-visual" aria-label="Duovillin esittelykuva">
      <div class="visual-frame">
        <img src="/images/kuva1.jpeg" alt="Duovilli esityksen lava" class="main-photo" />
        <div class="mini-gallery">
          <img src="/images/kuva2.jpeg" alt="Duovilli tanssi" class="thumb" />
          <video class="thumb video" muted autoplay loop playsinline poster="/images/kuva3.jpeg">
            <source src="/images/video1.mp4" type="video/mp4" />
          </video>
        </div>
      </div>
      <div class="floating-badge">
        <span>Seuraava esitys</span>
        <strong>18.10.2026</strong>
      </div>
    </div>
  </header>

  <main>
    <section class="section intro-grid" id="esitykset">
      <div class="section-heading narrow">
        <p class="eyebrow">DUOVILLI</p>
        <h2 class="eyebrow">Liike, draama ja tunnelma yhdessä.</h2>
      </div>
      <div class="feature-grid">
        <article class="feature-card">

          <h3>Esitykset</h3>
          <p>Original-tanssiteatteria, joka yhdistää rytmin, ilmeen ja intensiivisen vuorovaikutuksen.</p>
        </article>
        <article class="feature-card">  
          <h3>Työpajat</h3>
          <p>Liikkeen, kehon ja tarinankerronnan opetus sekä yhteisölliset kokemukset kaikille ikäryhmille.</p>
        </article>
        <article class="feature-card">
          <h3>Tapahtumat</h3>
          <p>Festivaalit, juhlat ja teatteritilaisuudet, joissa Duovilli tekee kaiken näkyväksi.</p>
        </article>
      </div>
    </section>

    <section class="section story-section" id="tarina">
      <div class="story-copy">
        <p class="eyebrow">TARINA</p>
        <h2>Duovilli rakentaa esityksiä, jotka puhuvat tunteella.</h2>
        <p>
          Jokainen koreografia alkaa liikkeestä, mutta kasvaa tarinaksi. Duovilli tuo lavalle rytmistä,
          ilmaisusta ja katsojan kanssa eläytyvästä hetkestä syntyvän kokemuksen.
        </p>
        <ul class="check-list">
          <li>Elävä ja tunnelmallinen tanssiteatteri</li>
          <li>Yksilölliset esitykset tilaisuuksiin</li>
          <li>Vuorovaikutteinen ja visuaalinen ilme</li>
        </ul>
      </div>
      <div class="story-photo">
        <img src="/images/kuva3.jpeg" alt="Duovillin performanssi" />
      </div>
    </section>

    <section class="section" id="tapahtumat">
      <div class="section-heading">
        <p class="eyebrow">TAPAHTUMAT</p>
        <h2>Esityksiä, jotka kutsuvat katsojan mukaan.</h2>
      </div>
      <div class="show-grid">
        <article class="show-card">
          <img src="/images/kuva1.jpeg" alt="Aamun esityksen kuva" />
          <div class="show-copy">
            <span class="tag">Festivaali</span>
            <h3>Yön valo</h3>
            <p > Rytmi, valo ja hiljaisuuden hetket yhdistyvät lämminhenkiseksi tanssiteatteriesitykseksi.</p>
          </div>
        </article>
        <article class="show-card">
          <img src="/images/kuva2.jpeg" alt="Yhteinen työpaja" />
          <div class="show-copy">
            <span class="tag">Työpaja</span>
            <h3>Liikettä ja tarinaa</h3>
            <p>Yhteisöllinen työpaja, jossa kehollisuus ja ilmaisu avautuvat yhdessä luovana prosessina.</p>
          </div>
        </article>
        <article class="show-card">
          <img src="/images/kuva3.jpeg" alt="Teatteriesitys" />
          <div class="show-copy">
            <span class="tag">Esitys</span>
            <h3>Tuuli sisällä</h3>
            <p>Voimakas visuaalinen tanssiteatteriesitys, joka jäsentää tunteita liikkeen kautta.</p>
          </div>
        </article>
      </div>
    </section>
  </main>

  <footer class="cta" id="yhteystiedot">
    <div>
      <p class="eyebrow light">Tilaa esitys</p>
      <h2>Duovilli tuo ilon, energian ja tunteen lavalle.</h2>
    </div>
    <a class="primary-button" href="mailto:hello@duovilli.fi">hello@duovilli.fi</a>
  </footer>
</section>

<style>
  :global(body) {
    margin: 0;
    background:
      radial-gradient(circle at top, rgba(255, 255, 255, 0.04), transparent 22%),
      linear-gradient(180deg, #000000 0%, #111111 30%, #050505 100%);
    color: #ffffff;
    font-family: 'Belgrano', serif;
  }

  * {
    box-sizing: border-box;
    font-family: 'Belgrano', serif;
  }

  a {
    text-decoration: none;
  }

  img,
  video {
    max-width: 100%;
    display: block;
  }

  .showcase {
    max-width: 1220px;
    margin: 0 auto;
    padding: 24px 24px 60px;
  }

  .topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    padding: 16px 22px;
    border-radius: 20px;
    background: rgba(0, 0, 0, 0.82);
    border: 1px solid rgba(255, 255, 255, 0.12);
    backdrop-filter: blur(18px);
    position: sticky;
    top: 14px;
    z-index: 10;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 800;
    letter-spacing: -0.04em;
    color: #f8efe9;
  }

  .brand-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 10px;
    background: #ffffff;
    color: #000000;
    font-size: 0.95rem;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 24px;
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.95rem;
  }

  .nav-links a {
    color: inherit;
  }

  .primary-button,
  .secondary-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 0.9rem 1.4rem;
    font-weight: 700;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .primary-button {
    background: #000000;
    color: #ffffff;
    box-shadow: 0 18px 38px rgba(0, 0, 0, 0.38);
  }

  .secondary-button {
    background: #ffffff;
    color: #000000;
    border: 1px solid rgba(255, 255, 255, 0.18);
  }

  .primary-button:hover,
  .secondary-button:hover {
    transform: translateY(-1px);
  }

  .hero {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    align-items: center;
    gap: 42px;
    padding: 66px 0 26px;
  }

  .eyebrow {
    margin: 0 0 16px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #ffffff;
    font-weight: 800;
    font-size: 0.72rem;
  }

  .eyebrow.light {
    color: rgba(255, 255, 255, 0.82);
  }

  h1 {
    margin: 0;
    font-size: clamp(3rem, 5vw, 6rem);
    line-height: 1.05;
    letter-spacing: -0.07em;
    color: #ffffff;
  }

  .lead {
    max-width: 620px;
    margin-top: 24px;
    color: rgba(255, 255, 255, 0.9);
    font-size: 1.08rem;
    line-height: 1.8;
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 30px;
  }

  .stats {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 32px;
    padding: 0;
    margin: 36px 0 0;
  }

  .stats li {
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .stats strong {
    font-size: clamp(1.7rem, 2vw, 2rem);
    letter-spacing: -0.06em;
  }

  .stats span {
    color: rgba(255, 255, 255, 0.8);
  }

  .hero-visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 470px;
  }

  .visual-frame {
    width: min(100%, 510px);
    display: grid;
    gap: 16px;
    padding: 18px;
    border-radius: 30px;
    background: rgba(0, 0, 0, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.45);
  }

  .main-photo {
    width: 100%;
    height: 300px;
    object-fit: cover;
    border-radius: 24px;
  }

  .mini-gallery {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .thumb {
    width: 100%;
    height: 180px;
    object-fit: cover;
    border-radius: 20px;
  }

  .video {
    background: #201612;
  }

  .floating-badge {
    position: absolute;
    right: 10px;
    bottom: 20px;
    display: grid;
    gap: 6px;
    background: rgba(36, 23, 18, 0.9);
    color: #fffaf6;
    border-radius: 18px;
    padding: 16px 18px;
    box-shadow: 0 18px 40px rgba(32, 18, 14, 0.18);
  }

  .floating-badge span {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: rgba(255, 255, 255, 0.7);
  }

  .floating-badge strong {
    font-size: 1.3rem;
    letter-spacing: -0.05em;
  }

  .section {
    padding-top: 74px;
  }

  .section-heading {
    margin-bottom: 26px;
  }

  .section-heading h2 {
    margin: 0;
    font-size: clamp(2rem, 3vw, 3rem);
    letter-spacing: -0.05em;
  }

  .narrow {
    max-width: 620px;
  }

  .feature-grid,
  .show-grid {
    display: grid;
    gap: 22px;
  }

  .feature-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .feature-card,
  .show-card {
    background: linear-gradient(180deg, #050505, #111111);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 24px;
    padding: 26px 22px;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.25);
  }

  .feature-card h3,
  .show-copy h3 {
    margin: 16px 0 12px;
    font-size: 1.35rem;
    letter-spacing: -0.04em;
    color: #ffffff;
  }

  .feature-card p,
  .show-copy p,
  .story-copy p,
  .check-list {
    color: #ffffff;
    line-height: 1.75;
  }

  .icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border-radius: 12px;
    background: linear-gradient(135deg, rgba(240, 178, 118, 0.18), rgba(165, 82, 66, 0.2));
    color: #f0b276;
    font-weight: 800;
  }

  .story-section {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 28px;
    align-items: center;
    padding: 60px 0 14px;
  }

  .story-copy h2 {
    margin: 0 0 18px;
    font-size: clamp(2.2rem, 3vw, 3.3rem);
    letter-spacing: -0.05em;
    color: #ffffff;
  }

  .check-list {
    list-style: none;
    padding: 0;
    margin: 24px 0 0;
    display: grid;
    gap: 10px;
  }

  .check-list li {
    position: relative;
    padding-left: 22px;
  }

  .check-list li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #ffffff;
    font-size: 1.4rem;
    line-height: 1;
  }

  .story-photo img {
    width: 100%;
    height: 520px;
    object-fit: cover;
    border-radius: 28px;
    box-shadow: 0 26px 50px rgba(37, 23, 17, 0.12);
  }

  .show-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .show-card {
    overflow: hidden;
    padding: 0;
  }

  .show-card img {
    width: 100%;
    height: 220px;
    object-fit: cover;
  }

  .show-copy {
    padding: 22px;
  }

  .tag {
    display: inline-flex;
    padding: 0.42rem 0.7rem;
    border-radius: 999px;
    background: #ffffff;
    color: #000000;
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .cta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    background: linear-gradient(135deg, #000000, #111111 45%, #080808 100%);
    color: #ffffff;
    border-radius: 28px;
    margin-top: 64px;
    padding: 38px 30px;
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  .cta h2 {
    margin: 0;
    font-size: clamp(2rem, 3vw, 2.8rem);
    letter-spacing: -0.05em;
    color: #ffffff;
  }

  @media (max-width: 960px) {
    .hero,
    .story-section,
    .show-grid,
    .feature-grid,
    .cta {
      grid-template-columns: 1fr;
    }

    .hero,
    .story-section {
      display: grid;
    }

    .topbar {
      flex-wrap: wrap;
      justify-content: center;
    }

    .nav-links {
      order: 3;
      width: 100%;
      justify-content: center;
      flex-wrap: wrap;
    }

    .cta {
      display: grid;
    }
  }

  @media (max-width: 640px) {
    .showcase {
      padding-left: 16px;
      padding-right: 16px;
    }

    .topbar {
      padding: 14px 16px;
      border-radius: 16px;
    }

    .nav-links {
      gap: 12px;
      font-size: 0.8rem;
    }

    .actions {
      flex-direction: column;
      align-items: stretch;
    }

    .primary-button,
    .secondary-button {
      width: 100%;
    }

    .stats {
      flex-direction: column;
      gap: 16px;
    }

    .main-photo {
      height: 240px;
    }

    .thumb {
      height: 140px;
    }

    .story-photo img {
      height: 360px;
    }
  }
</style>
